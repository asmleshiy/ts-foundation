import { default as utils } from './libs/utils'

export const toJson = utils.toJson
export const fromJson = utils.fromJson
export const deepCopy = utils.deepCopy
export const isNil = utils.isNil
export const exact = utils.exact
